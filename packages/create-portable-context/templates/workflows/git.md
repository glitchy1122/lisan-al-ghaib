# Workflow: Git

- Prefer conventional commits: `feat:`, `fix:`, `docs(context):`, `docs(handoff):`
- Push before switching environments
- Never commit secrets
- Keep handoff truthful about uncommitted work if you must leave mid-change
