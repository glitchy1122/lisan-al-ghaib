@AGENTS.md

# Claude Code notes

- Prefer updating `docs/handoff/CURRENT.md` at the end of every turn that changes code.
- Use skills under `.agents/skills/` when the task matches (session-handoff, update-context, write-adr, git-workflow).
- Nested package instructions: `apps/web/AGENTS.md`, `packages/shared/AGENTS.md`.
